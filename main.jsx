import React from "react";
import ReactDOM from "react-dom/client";
import HydraulicSimulation from "./HydraulicSimulation";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HydraulicSimulation />
  </React.StrictMode>
);
